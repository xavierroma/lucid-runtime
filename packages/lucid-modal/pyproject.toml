[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "lucid-modal"
version = "0.1.0"
description = "Modal adapter for Lucid runtimes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115,<1",
  "modal>=1.3.5",
  "pydantic>=2.8,<3",
  "lucid-runtime>=0.1,<0.2",
]

[project.optional-dependencies]
test = [
  "pytest>=8.3,<9",
  "pytest-asyncio>=0.24,<0.25",
]

[project.scripts]
lucid-modal = "lucid_modal.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
package = true

[tool.uv.sources]
lucid-runtime = { path = "../lucid", editable = true }
